return {
  "folke/snacks.nvim",
  opts = {
    terminal = {
      auto_insert = false, -- boolean start insert mode when entering the terminal buffer
      auto_close = true, -- close the terminal buffer when the process exits
    },
  },
}
