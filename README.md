# Dotfiles




```
cd ~
git clone git@github.com:docwhite/.dotfiles
ln -s .dotfiles/.config
ln -s .dotfiles/.zshrc
```

The terminal used on macOS and linux is [Alacritty](https://github.com/alacritty/alacritty):


```
brew install alacritty
```
